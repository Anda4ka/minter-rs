# Быстрый запуск MINTER

**Создатель:** [X @AndarkFomo](https://x.com/AndarkFomo) · [Telegram @grassfoundationn](https://t.me/grassfoundationn)

Полная инструкция (пошагово):

**→ [`Public/ИНСТРУКЦИЯ.md`](Public/ИНСТРУКЦИЯ.md)**

---

## Совсем коротко

1. Откройте папку **`Public\`** (или распакуйте zip).  
2. Запустите **`minter-desktop.exe`**.  
3. Unlock vault (burner only + пароль).  
4. **Wallets** → импорт ключей → (опц.) **Network** + **Check balances**.  
5. **Settings** → Alchemy API key (private) / RPC → Save → **RPCs** → **Ping networks**.  
6. (Желательно) **Proxies** → список → Save.  
7. **Tasks** → Create task → **Start** (**LIVE** + type **`LIVE`**; HUD Mission Control).  
8. **Raw Mint** — multi-wallet contract (`mint(uint256)` / Discover) — см. ИНСТРУКЦИЯ.

**Success = CONFIRMED в блоке.** `SENT` — ещё не победа.  
Open фазы: **wall clock** (не ждём lag chain ~12s).

---

## Что нужно / не нужно

| Путь | Нужен для запуска? |
|------|---------------------|
| `Public\minter-desktop.exe` + данные рядом | **Да** |
| `target\` | **Нет** (только кэш сборки) |

---

## Сборка из исходников

После правок кода:

```powershell
# из корня репо
powershell -ExecutionPolicy Bypass -File scripts\package-public.ps1
.\Public\minter-desktop.exe

# безопасный zip для друзей (без vault/config/results):
powershell -ExecutionPolicy Bypass -File scripts\package-public.ps1 -MakeZip
```

Скрипт: `cargo build -p minter-desktop --release` → копирует exe в `Public\`.  
Не затирает vault / config / tasks. `-MakeZip` пакует **только** allowlist (`SHIP_MANIFEST.txt`).

Освободить диск после package:

```powershell
Remove-Item -Recurse -Force target
```

Тесты:

```powershell
cargo test -p minter-core
```

---

## Документация

| Файл | Содержание |
|------|------------|
| [`Public/ИНСТРУКЦИЯ.md`](Public/ИНСТРУКЦИЯ.md) | Полный гайд (Wallets/RPC/Raw/MC) |
| [`Public/README.txt`](Public/README.txt) | Содержимое zip (EN) |
| [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) | Архитектура, RPC, Discover |
| [`docs/UI_PLAN.md`](docs/UI_PLAN.md) | UI: балансы, Ping networks, Mission Control |
| [`docs/CODE_AUDIT.md`](docs/CODE_AUDIT.md) | Аудит кода |
| [`docs/BUGFIX_PLAN.md`](docs/BUGFIX_PLAN.md) | Статус runtime-фиксов |
| [`docs/RISK_MITIGATION_PLAN.md`](docs/RISK_MITIGATION_PLAN.md) | Риски |
| [`docs/IMPROVEMENT_PLAN.md`](docs/IMPROVEMENT_PLAN.md) | Планы улучшений |
